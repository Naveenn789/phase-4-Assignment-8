@model IEnumerable<WebAppBooks.Models.Publisher>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Paddress)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Pid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Paddress)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Pname">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Pname">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Pname">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
